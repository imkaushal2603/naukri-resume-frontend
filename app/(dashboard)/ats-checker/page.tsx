import Loader from "@/components/ui/Loader";

export default function ATSChecker() {
    return (
        <div className="relative">
            {/* {loading && <Loader overlay />} */}
            <div className="flex flex-wrap gap-6">
                <h1>ATS Checker</h1>
                <p>This is the ATS Checker page.</p>
            </div>
        </div>
    )
}